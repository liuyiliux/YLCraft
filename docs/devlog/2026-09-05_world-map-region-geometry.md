# 2026-09-05 世界地图：样式改造 + 区域几何重构交接

## 一、本轮做了什么（按时间顺序）

### 1. 样式规范先行（用户要求"生成前先写清楚整体风格"）

新增 `docs/design/world-map-workbench-style.md`（11 章）：整体风格、信息架构与骨架尺寸、
色彩令牌（浅/深）、字体排版、间距密度、关键组件规格、图标、交互与状态、可访问性与响应式、
实施清单、改前改后对照。**约定：改样式前先改这份文档，代码是文档的结果。**

### 2. 画布视觉语言：从"军事沙盘"改为"小说地图"

用户反馈"像行军打仗，不像小说里的地方"。定位三个元凶并全部换掉：

| 元凶 | 改前 | 改后 |
|---|---|---|
| 据点是胶囊色块（像部队番号牌） | 圆角色块 + 白字 | 按 kind 的**地物图标** + 衬线地名标签（纸色描边） |
| 区域是高饱和实色块（像占领区） | UI 原色 `#1677ff/#eb2f96` | **地形色系**（苔绿/土黄/水蓝/赭石等低饱和），填充 `.08` + 虚线疆界 |
| 路线是直线（像行军箭头） | 两点直连 | **手绘曲线**（二次贝塞尔采样），按类型线型配色 |

配套：纸色底 + 坐标网、画布随视口长满、罗盘玫瑰、自定义缩放条、坐标读数、
左栏图例（与画布同色源）、浅深主题切换。

### 3. 三态完整 + 标签 LOD

- 画布空态三种语义（整图无据点 / 位面或筛选为空 / 图层全关），都给"去哪做第一件事"。
- 游离标记（无 entity_id）与实体缺失分别给警告条，说明"正典应写在 world_entities"。
- 标签分级：缩小只显示区域名，放大才出据点名，选中项始终可见（借鉴 Azgaar）。

### 4. 画布主题 / 做旧滤镜 / 视图预设

纸张 / 水墨 / 夜晚三主题（纯 CSS 变量 + filter）；画风预设与主题联动；
视图预设（政治 / 自然 / 纯结构）一键切图层组合。

### 5. 区域几何重构（进行中，本次的核心）

见下节。

### 6. 阶段 3 收尾（第二批接手后完成）

- **顶点增删**：双击边在最近边处加点（`nearestEdgeIndex` 纯函数定位，编辑态自动关闭地图
  双击缩放，64 顶点上限拒绝）、右键顶点删点（≥3 顶点保底）；改动照旧固化为 `manual`。
- **层级树与父区域**：左栏区域列表改为树序缩进（可折叠）+ 行内父区域选择；
  新增 `utils/regionHierarchy.ts`（`computeRegionDepths` / `canReparent` / `regionDisplayOrder`），
  成环/自指/超深在写入口统一拦截（面板选项置灰 + 批量编辑写前拦截），脏数据展示层平铺兜底不丢行。
- **据点越界警告**：`pointInPolygon` 射线法（凹多边形正确、边界不算越界），
  画布上方警告条 + 据点详情警告框；引用不变，区域未生成形状时不判定。
- 验证：tsc 双配置 0 错误、vitest **27/27**（新增 18 项：越界 4 / 最近边 3 / 层级 11）、
  页面冒烟通过、vite build 通过。

## 二、区域几何重构：为什么要改（这是语义错误，不是样式问题）

用户原话：*"福贵的村子是个区域，但福贵的房子不是区域的角——我们会把它设为区域的角，
实际应该是城墙角之类的。"*

**旧实现的错误**：区域渲染为「成员据点围成的凸包」——等于把"房子"当成"村子的城墙角"。
后果：据点一动形状就变形；凸包画不出凹形（村子绕过山头的边界、海湾、城墙拐角）。

**正确层级**：

```
区域（有形状的地理范围）  ← 村子、县城、势力范围
  └─ 据点（区域内的一个位置） ← 福贵的房子、村口老槐树
```

形状是区域的**独立几何**；据点通过 `region_id` **引用**区域，不决定其形状——
与既有「引用不复制」纪律一致。

## 三、已确认的关键决策（13 项）

完整记录在 `.ai-sdd/requirements/region-geometry-rework/discover-2026-09-04.md`（状态：已确认）：

1. 形状来源：双模式（一键生成 + 手绘微调）
2. 归属：**引用为准，位置仅提示**（据点画到形状外只警告，不自动改引用）
3. 生成方式：**可控生成**（不接 Azgaar 随机生成管线，形状由我们的数据确定性展开）
4. 旧数据：直接清库，不备份（已确认；执行前会再报数量）
5. AI 输出：**语义参数 + seed**，不输出几何
6. 触发：独立动作（用户点 / Agent 调工具）
7. 顶点上限：64
8. 层级视觉：父淡子艳，选中更艳
9. 清理范围：全部清空 `world_maps` + `world_map_revisions`
10. 嵌套：不限层，深度自动算视觉权重
11. 词表：三维度（自然意象 8 × 聚落形态 6 × 人工构筑 3+）
12. 据点 kind：扩到 20 种 + 每种配图标
13. 许可证：Azgaar 为 **MIT**，参考其算法无法律障碍

**D-1（重要）**：几何由**前端唯一实现**，后端与 Agent **不实现几何**。
AI/Agent 只产语义参数，顶点一律由 `frontend/src/utils/regionShape.ts` 展开——
单一实现、零双份维护、天然一致。

## 四、进度（详细勾选在 `openspec/changes/region-geometry-rework/specs/region-geometry/tasks.md`）

| 阶段 | 状态 |
|---|---|
| 1 数据与算法地基 | ✅ 形状展开算法 + 类型 + 回归测试 |
| 2 画布渲染 | ✅ 画真形状 + 层级视觉 + 未生成形状的内存兜底 |
| 3 交互 | ✅ 生成/重新生成/拖顶点/双击加点/右键删点 + 层级树与父区域校验 + 越界警告条 |
| 4 后端与 Agent | ❌ 未开始 |
| 5 据点 20 种 + 图标 | ❌ 未开始 |
| 6 清库与文档 | ❌ 未开始（架构文档已更新，本文件即交接） |

**推送状态（2026-09-05 第二批更新）**：CNB（`origin`，cnb.cool）已同步到 `ea2989d5`；
**GitHub 仍落后**——直连持续被重置，代理可用后 `git push github main` 一次推完。

## 五、接手者必须知道的坑

1. **不要照旧文档/旧代码写"据点围合"**——架构文档已改为新语义（`docs/architecture/YLCRAFT_SYSTEM_ARCHITECTURE.md`
   搜索"区域几何"）。旧凸包逻辑已从 `MapCanvas` 移除。
2. **形状算法是纯函数且有测试**：改参数先跑 `npm test`（frontend 已装 vitest 1.6.0，现 27 项）。
   测试覆盖确定性、顶点上限、坐标裁剪、**据点必被包住**、面积感递增、越界判定、层级成环校验等。
3. **基础半径公式别改错**：`最远据点距离 × 1.15 + 面积感半径`——先包住据点再按面积感外扩。
   早期版本写成 `max(面积感, 离散度)`，导致据点一分散"小=中"（面积感失效），已修正并被测试覆盖。
4. **编辑工具有时会"看似成功但未落盘"**：本轮发生过两次 `replace_in_file` 返回成功但文件未变。
   改完关键文件后务必 `tsc` 或回读确认。
5. **清库是破坏性操作**，阶段 6 执行前必须再向用户确认一次。
6. **网络限制**：GitHub 直连常被重置（push / clone 都失败过），需要代理；
   npm 可用（vitest 已装上）。
7. **frontend 用了 unplugin-auto-import**：`src/auto-imports.d.ts` 全局声明了 React hooks 等，
   文件里不写 `import { useCallback } from 'react'` 也能编译——这不是丢导入，别"修复"它，
   也别因此怀疑 tsc 没跑对。项目 `tsc` 是 `strict: false`。

## 六、下一步建议（按顺序）

1. 阶段 4：后端形状参数校验 + AI 语义推断端点（`POST /world-maps/{id}/regions/{rid}/shape/generate`）
   + Agent 工具（`generate_region_shape`、`list_region_shape_presets`，只产语义参数）。
2. 阶段 5：据点 kind 扩到 20 种 + 图标。
3. 阶段 6：清库脚本（确认后执行）+ 文档收尾。
