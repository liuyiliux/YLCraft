# 知识回流报告：character-management-redesign

- 日期：2026-09-13
- 来源 change：`openspec/changes/character-management-redesign/`（**65/65 完成**，待归档）
- 素材：tasks.md（65 项）/ 实现代码与真实浏览器实测
- 代码范围：`frontend/src/pages/character-detail/`（`index.tsx` / `styles.css` / `components/`）、
  `backend/app/api/v1/characters.py`、`CharacterRelationship`（迁移 `025`）、`field_sources_json`（迁移 `024`）、
  提取来源 `027`
- **注入目标现状**：上下文库已有角色相关零散记录；本次补**角色册完善度**与**接口字段可用性**等实操结论。

---

## 一、回流概览

| 类别 | 新增 | 补充 | 重复（跳过） | 冲突 |
|------|------|------|------------|------|
| 业务规则 | 3 | 0 | 2 | 0 |
| 数据表/字段 | 2 | 0 | 1 | 0 |
| 枚举值 | 2 | 0 | 1 | 0 |
| 工程约束 | 4 | 0 | 0 | 0 |
| **合计** | **11** | **0** | **4** | **0** |

---

## 二、业务规则（3 项 ✅ 新增）

| 规则 | 内容 |
|---|---|
| **角色有两类流程，门禁不同** | `extract`（从小说/正文提取）与 `character-first`（角色先行再演绎）**不共享**强制正文/大纲门禁；角色先行可直接进入设定、参考图、关系与 Prompt 资产包，再选择性回流 Story/生产线 |
| **字段来源分层** | `field_sources`：外来文本 → `original`、原创大纲 → `ai_inferred`、用户手填 → `user_edited`；**同步流程只补空缺，不覆盖已有来源** |
| **角色册完善度只用列表字段** | 完善度与摘要必须由**列表接口已返回**的字段计算，不得逐项请求详情——窄栏里为每个角色各发一次详情请求，在角色多时会明显拖慢列表 |

---

## 三、数据表/字段（2 项 ✅ 新增）

- **`CharacterRelationship`**（迁移 `025`）：`character_id` / `related_character_id` / `relation_type` / `relation_note` / `source` / `is_directed`
- **`field_sources_json`**（迁移 `024`，text 默认 `{}`）：字段级来源标记；提取来源细分 `extract_origin`（`uploaded_novel` / `imported_novel` / `original_outline`，落在 `character_story_links`，迁移 `027`）
- **`GET /api/v1/characters` 列表返回 38 个字段**（实测），含 `identity` / `ability` / `arc` / `behavior` / `motivation` / `speech` / `voice` 等 Bible 分节，以及 `appearance` / `personality` / `background` / `age_range` / `portrait_url` / `voice_asset_id` / `reference_asset_ids` / `poses` / `expressions` / `tags` / `field_sources` / `extract_origins` —— **足以在列表侧算完善度，无需详情**。

---

## 四、枚举值（2 项 ✅ 新增）

- **`workflow_source`**：`extract`（小说/正文提取）/ 独立创建 / 素材库导入（另有 `workflow_source_label` 展示用）
- **`extract_origin`**：`uploaded_novel` / `imported_novel` / `original_outline`

---

## 五、工程约束与踩坑（4 项 ✅ 新增）

1. **Bible 分节字段类型不统一**：`identity` / `ability` / `behavior` / `motivation` / `speech` / `voice` 是**对象**，但 `arc` **既可能是字符串也可能是对象**（实测 11/20 填充且类型混杂）。判空与渲染都必须两种都处理，否则会把已填的 `arc` 当成空。
2. **字段填充率差异很大**（实测 20 个角色）：`background` 20/20、`personality` 18/20、`identity` 15/20、`appearance`/`age_range` 11/20、`arc` 11/20、`ability`/`behavior`/`motivation`/`speech` 各 3/20、`voice` **0/20**。**完善度阈值与"缺失项"提示要按这个分布设计**，否则要么全员"完善"、要么全员"未完善"。
3. **`identity` 对象的键不固定**：出现过的键有 `summary`(10) / `affiliation`(10) / `alias`(3) / `gender`(3) / `species`(3) / `organization`(3) / `position`(3) / `logline`(3) / `visual_profile`(5)。取摘要要按优先级链兜底，不能假定某个键一定存在。
4. **窄栏视觉弱化要给选中态留出口**：未完善角色整体降不透明度会被误读为"不可点"；**hover 与选中态必须恢复满不透明度**。实测验收时若只取第一个 `.is-thin` 元素会拿到选中项（opacity=1）而误判"弱化没生效"——断言需排除选中态。

---

## 六、冲突项

无。65 项任务全部完成，`openspec validate --strict` 通过。

**2026-09-13 实测**：`/characters` 20 个角色、20 个完善度条（首个 22%（2/9）并列出缺失项）、20 个 Bible 摘要（单行省略样式确认）、9 个 `is-thin`（非选中项 opacity 0.62）；独立角色页 / 关系图谱 / Prompt 资产包均正常；0 个 >=400、0 条控制台 error；`tsc --noEmit` 与 `npm run build` 均通过。

---

## 七、合并计划

| # | 操作 | 目标文件 |
|---|------|---------|
| 1 | 追加角色册与完善度规则 | `business/业务规则与核验体系（business-rules）.md` |
| 2 | 追加 `CharacterRelationship` / `field_sources_json` / 列表 38 字段 | `data/核心数据模型（data-model）/`（追加到既有角色相关文件或新子文件） |
| 3 | 追加 `workflow_source` / `extract_origin` 枚举 | `data/枚举值字典（enum-dictionary）.md` |
| 4 | 追加 4 条工程约束（Bible 分节类型不一、填充率分布、identity 键不固定、弱化要给选中态留出口） | `engineering/设计约束（design-conventions）.md` |

---

## 八、索引更新建议

- `"涉及角色册、完善度、Bible 摘要"`
- `"涉及角色两类流程（提取 vs 角色先行）与字段来源"`
