# 知识回流报告：creative-project-writing-guardsrails

- 日期：2026-09-12
- 来源 change：`openspec/changes/creative-project-writing-guardrails/`（9/9 完成，待归档）
- 素材：design.md / tasks.md / specs/writing-guardrails/spec.md + 实现代码核对
- 代码范围：`backend/app/api/v1/creative_projects.py:818`（路由）、`backend/app/services/creative_project/service.py:5635`（服务）、
  `backend/app/skills/novel/chapter-hook-rhythm/SKILL.md`（方法包）、`frontend/src/api/index.ts:1984`、
  `backend/tests/test_creative_project_service.py`（10 处 preflight 断言）
- **注入目标现状**：`.ai-sdd/context/` 已覆盖任务/事件观测、写作风格、Agent 工作台前端、世界构建；
  **写作前置检查与 Creative Skill 方法包**为空白，故多数条目标记为「新增」。

---

## 一、回流概览

| 类别 | 新增 | 补充 | 重复（跳过） | 冲突 |
|------|------|------|------------|------|
| 术语与业务对象 | 4 | 0 | 0 | 0 |
| 业务规则 | 5 | 0 | 0 | 0 |
| API | 1 | 0 | 0 | 0 |
| 数据表/字段 | 1 | 0 | 0 | 0 |
| 枚举值 | 1 | 0 | 0 | 0 |
| 工程约束 | 4 | 0 | 0 | 0 |
| 设计决策 | 3 | 0 | 0 | 0 |
| **合计** | **19** | **0** | **0** | **0** |

---

## 二、术语与业务对象（4 项 ✅ 新增）

| 术语 | 定义 | 证据 |
|---|---|---|
| **Writing Preflight（写作前置检查）** | 对已持久化项目状态的**只读投影**：不创建上下文快照、不调用模型，只回答"现在能不能写这一步" | `service.py:5635` |
| **方法包（Method Package）** | 文件型 Creative Skill，以 `SKILL.md` 形式存在并被 preflight 返回（含 id 与 checksum） | `backend/app/skills/novel/chapter-hook-rhythm/SKILL.md` |
| **`chapter-hook-rhythm`** | 首个章节钩子与节奏方法包；**opt-in**（`auto_apply=false`），仅在被选中时向 T6 贡献方法指导 | design.md |
| **T6** | Context Pack 第 6 层（表达机制注入层）；方法包只作用于此层，不改变正典边界 | design.md |

---

## 三、业务规则与校验逻辑（5 项 ✅ 新增）

| 规则 | 内容 | 证据 |
|---|---|---|
| preflight 必须只读 | 不创建 `ProjectNarrativeContextSnapshot`、不调用模型；仅投影既有状态 | design.md |
| 返回值契约固定 | 归一化 `stage` 与 `chapter_number`、有序 `checks`（`pass`/`block`）、`ready`、`blockers`、可执行的 `next_action`、可用章节大纲 source id、兼容方法与 checksum | design.md |
| 生成方法仍是权威 | preflight 只做前置判断，不替代生成逻辑；未就绪时应解释并修复而非强行生成 | design.md |
| opt-in 方法包默认不生效 | `auto_apply=false`；须在项目设置中被选中才写入 T6 | design.md |
| 禁止性变更保留既有边界 | 方法包只贡献"怎么写"的方法指导，不突破候选/正典边界 | design.md |

---

## 四、API 知识（1 项 ✅ 新增）

| 接口 | 关键约定 |
|---|---|
| `GET /api/v1/creative-projects/{project_id}/writing-preflight?chapter=&stage=&source=` | 只读；返回 `{success, data}`，data 含 stage/chapter_number/checks/ready/blockers/next_action/source_id/methods。前端封装为 `getCreativeProjectWritingPreflight`（`src/api/index.ts:1984`） |

**同一契约两用**：真人 UI 在按钮启用前调用以禁用或标注被阻塞的动作；Agent 用同一份契约来解释并修复被阻塞的工作流。

---

## 五、数据表/字段知识（1 项 ✅ 新增）

- 项目设置中持久化**已选方法 ID 列表**；preflight 返回的每个方法带 `checksum`，用于判断方法包是否变更。

---

## 六、枚举与字典（1 项 ✅ 新增）

- **check 状态**：`pass` / `block`（`ready` = 无 blocker）。

---

## 七、工程约束与踩坑（4 项 ✅ 新增）

1. **Skill 以文件形式存放**：`backend/app/skills/{novel,creative}/<name>/SKILL.md`，`name:` 字段即方法 id（如 `chapter-hook-rhythm`）。新增方法包走同一约定，便于 preflight 自动发现。
2. **preflight 与 Agent 共用契约**：不要让 UI 另写一套"能不能点"的判断——否则 Agent 侧解释阻塞原因时会与前端不一致。
3. **⚠ 排查工具陷阱（本轮实际踩到）**：PowerShell 的 `Select-String -Path <目录> -Include *.py` **不会递归子目录**。审计时先用它搜 `chapter-hook-rhythm` 得到"不存在"，差点误判任务 #4 是假勾选；改用 `Get-ChildItem -Recurse | Select-String` 复查才发现文件就在 `backend/app/skills/novel/`。**判断"某物是否存在"必须递归搜索。**
4. **任务注释里可能已有验收证据**：本 change 的任务 #9 用日期注释记录了 2026-08-07/08 的 94→97 项测试与 1440px/390px 的 Patchright smoke 结果。审计时**先读任务下的注释**，不要只看标题措辞就判定"无证据"。

---

## 八、设计决策（3 项 ✅ 新增）

| 决策 | 理由 | 被否决的方案 |
|---|---|---|
| preflight 做成只读投影，而非生成动作或快照 | 不产生副作用、可随时调用；按钮禁用与 Agent 解释共用一份真相 | 预生成上下文快照（有副作用、会污染历史） |
| 方法包默认 opt-in（`auto_apply=false`） | 写作风格与方法指导应由人选择，避免悄悄改变输出风格 | 默认全部启用 |
| 真人 UI 与 Agent 共用同一 preflight 契约 | 保证"为什么不能写"的解释在两侧一致 | 前端另写一套判断 |

---

## 九、冲突项

无。路由、服务、方法包、前端封装、测试（10 处 preflight 断言）均已核对存在，与 9/9 任务一致。

---

## 十、合并计划

| # | 操作 | 目标文件 | 内容概要 | 优先级 |
|---|------|---------|---------|--------|
| 1 | 追加新节「写作前置检查」 | `.ai-sdd/context/business/领域术语与业务对象（domain-glossary）.md` | 4 个术语（preflight、方法包、chapter-hook-rhythm、T6） | 中 |
| 2 | 追加新节「写作前置检查规则」 | `.ai-sdd/context/business/业务规则与核验体系（business-rules）.md` | 5 条规则（只读、返回契约、生成权威、opt-in、保留边界） | 中 |
| 3 | 追加新节 | `.ai-sdd/context/engineering/接口设计规范（api-style-guide）.md` | preflight 端点与"UI/Agent 共用契约"约定 | 中 |
| 4 | 追加新节 | `.ai-sdd/context/engineering/设计约束（design-conventions）.md` | 3 条（Skill 文件约定、共用契约、**Select-String 不递归的检索陷阱**） | 高 |
| 5 | 追加新节 | `.ai-sdd/context/data/枚举值字典（enum-dictionary）.md` | check 状态 pass/block | 低 |

---

## 十一、索引更新建议

- `context-index.yaml` 为 `business-rules` / `api-style-guide` / `design-conventions` 增加触发，例如
  - `"涉及写作前置检查、writing-preflight、Creative Skill 方法包"`
  - `"需要判断某阶段能否生成、为什么被阻塞"`
- 本 change 知识量较小，不新增目录子文件。

---

## 十二、下一步

- [ ] 使用 `/context-update` 执行合并计划（5 项操作 + 索引更新）
- [ ] 归档 `creative-project-writing-guardrails`（落地 capability `writing-guardrails`，2 条需求）
