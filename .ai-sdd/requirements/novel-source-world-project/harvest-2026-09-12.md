# 知识回流报告：novel-source-world-project

- 日期：2026-09-12
- 来源 change：`openspec/changes/novel-source-world-project/`（50/50 完成，待归档）
- 素材：proposal.md / design.md / tasks.md / specs/novel-world-project/spec.md（12 条需求）+ 实现代码与 2026-09-12 实测
- 代码范围：`backend/app/api/v1/novel_sources.py`（约 40 路由）、`backend/app/services/novel_source/`、
  迁移 `032_add_novel_source_world` → `037_add_world_entities_and_relations`、`035`（WorldMapDocument）、
  `036`（embedding_vec）、`038`（world_domain_definitions）、`042`（world_map_revisions）、
  `backend/tests/test_novel_source_world.py`（44 例）、`frontend/src/pages/novel-world/`
- **注入目标现状**：上下文库已有「世界构建（AI 渐进生成）」域，但**小说来源 → 世界提取**这条链路为空白，
  故绝大多数条目为「新增」。

---

## 一、回流概览

| 类别 | 新增 | 补充 | 重复（跳过） | 冲突 |
|------|------|------|------------|------|
| 术语与业务对象 | 7 | 0 | 0 | 0 |
| 业务规则 | 8 | 0 | 0 | 0 |
| API | 2 | 0 | 0 | 0 |
| 数据表/迁移 | 2 | 0 | 0 | 0 |
| 枚举值 | 5 | 0 | 0 | 0 |
| 工程约束 | 4 | 0 | 0 | 0 |
| 设计决策 | 3 | 0 | 0 | 0 |
| **合计** | **31** | **0** | **0** | **0** |

---

## 二、术语与业务对象（7 项 ✅ 新增）

| 术语 | 定义 | 证据 |
|---|---|---|
| **来源快照（NovelSourceSnapshot）** | 导入的原文的**只读**版本化快照：元数据、校验和、章节顺序、原文件引用与稳定 source anchor | spec 需求 1 |
| **文本块（NovelTextChunk）** | 带溯源的有序切分单位，承载章节/偏移/校验和，是提取与检索的证据粒度 | 需求 3 |
| **混合检索** | 精确 + 顺序邻近 + **可选**向量三者组合；向量不可用时降级为精确/顺序，仍返回证据锚点 | 需求 3 |
| **分域提取运行（WorldExtractionRun）** | 逐域进度、重试、诊断、checkpoint 的持久化运行对象 | 需求 9 |
| **候选（WorldFactCandidate）** | 抽取产物，**先于正典**；含 `payload`、`evidence`、`confidence`、`origin`、`target_entity_type`、`target_entity_id` | 需求 5、10 |
| **`source_canon` 层** | 派生项目里**原作正典**的只读事实层，与 `confirmed_project_facts` / `derivative_delta` / `pending_candidates` 分层 | 需求 6 |
| **基础层与扩展域** | 基础层（角色/关系/地点/时间线/相关规则与物品/未决问题）**永远可用**；其余域（规则/经济/战力/地理/势力/物种/历史事件/物品/术语…）**逐个独立判定** | 需求 7 |

---

## 三、业务规则与校验逻辑（8 项 ✅ 新增）

| 规则 | 内容 |
|---|---|
| **证据必须逐字命中** | 抽取值必须**逐字**落在某个文本块/偏移上，否则该条目**直接丢弃**（不是降级保留） |
| **候选先于正典** | 模型抽取的设定**绝不自动成为正典**；必须先人工/Agent 确认 |
| **`apply` 是唯一写入点** | `POST /world-extraction-runs/{run_id}/apply` 是写入项目的**唯一**通道；未提供项目时自动创建世界项目 |
| **角色复用既有 Character** | 角色写入既有 `Character` + `CharacterStoryLink`，**不建平行的"小说世界角色"实体** |
| **复杂域物化为独立实体** | 势力/地点/物种/历史事件/战力/物品等物化为 `WorldEntity`（幂等 upsert）与 `WorldEntityRelation`，与 `world_asset` 事实卡**并存**（事实卡仍是锁定正典的权威载体） |
| **只有完本来源可派生** | 派生项目（adaptation / continuation / fan_work）**仅对完本来源开放**；来源快照**始终只读** |
| **连载走增量同步** | `sync` 只追加新章节与新块，**既有章节偏移与证据锚点保持稳定**；增量提取从 checkpoint 起，只喂新块 |
| **向量失败不阻断** | 未配置或向量失败时，证据校验**稳定降级**为精确/顺序检索，不回退失败 |

---

## 四、API 知识（2 项 ✅ 新增）

| 接口组 | 关键端点 |
|---|---|
| **来源与检索** | `POST /novel-sources/import-txt`、`import-bookshelf`、`GET /novel-sources`、`/{id}`、`/chapters`、`/chunks`、`POST /{id}/chunks/index`、`/chunks/search` |
| **提取与写入** | `POST /{id}/plan`（逐域判定）、`POST /{id}/extract`、`GET /world-extraction-runs/{run_id}`、`/candidates`、`/reconcile`、`POST /candidates/decide`、`POST /apply`、`POST /contradictions`、`POST /affected-facts` |
| **派生与地图** | `POST /{id}/derive`、`/{id}/sync`、`POST /creative-projects/from-novel-source`、`GET /projects/{id}/world-entities`、`/world-entity-relations`、`GET /creative-projects/{id}/world-knowledge`、`/world-maps/*`（CRUD / render / export / revisions / rollback / generate-visual） |

- Agent 侧同名能力：`list_novel_source_snapshots`、`inspect_novel_source_snapshot`、`plan_novel_source_domains`、
  `extract_novel_source_world`、`list_world_extraction_candidates`、`decide_world_extraction_candidates`、
  `apply_world_extraction_run`、`reconcile_world_extraction_run`、`detect_world_extraction_contradictions`、
  `propagate_affected_world_facts`、`sync_novel_source_chapters`、`index_novel_source_chunks`、
  `search_novel_source_chunks`、`derive_project_from_novel_source`、`world_map_tools` 组（12 个）。
  **命名陷阱**：候选决策/写入类工具名含 `world_extraction` 而**不含** `novel_source`，按 `novel_source` 搜索会漏。

---

## 五、数据表/迁移（2 项 ✅ 新增）

| 迁移 | 内容 |
|---|---|
| `032_add_novel_source_world` | 来源快照 / 章节 / 文本块骨架 |
| `035` | `WorldMapDocument`（区域/据点/路线的结构化空间关系） |
| `036` | `embedding_vec vector(384)`（**仅 PostgreSQL**，pgvector） |
| `037_add_world_entities_and_relations` | `WorldEntity` + `WorldEntityRelation`（幂等 upsert） |
| `038` | `world_domain_definitions`（项目级域定义，`builtin_override`/`custom`/`ai_suggested`） |
| `042` | `world_map_revisions`（append-only 版本历史） |

**关键：地图空间关系（`map_json`）才是正典，AI 成图只是派生视觉资产**（只以引用形式记在 `map_json.visuals`）。

---

## 六、枚举与字典（5 项 ✅ 新增）

- **`SourceStatus`**：`completed` / `serial` / `unknown`
- **`DerivationKind`**：`adaptation` / `continuation` / `fan_work`
- **候选状态**：`pending` / `accepted` / `ignored` / `merged`
- **域判定（detection）**：`detected` / `uncertain` / `not_detected`（另有 disabled / user_requested）
- **域 `source` 与 `fact_layer`**：`builtin_override` / `custom` / `ai_suggested`；`source_canon` 等

---

## 七、工程约束与踩坑（4 项 ✅ 新增）

1. **序列化大纲也必须满足"逐字证据"**：把项目大纲转成来源文本时，每个字段要带 `【节】` 标记，使 AI 引用的引文能逐字落在该文本上从而通过证据校验（与小说来源同一套规则）。
2. **`Select-String` 不递归子目录**：本项目检索代码时若只给目录，会漏掉子目录里的实现，得出"没实现"的错误结论。**核对实现必须用递归搜索**。
3. **内置域字段只可追加、不可删除**：`world_domain_definitions` 允许覆盖标签/提示词、追加字段、禁用模块、添加自定义模块，但内置字段**禁止删除**——否则既有 `attributes_json` 会解析失败。
4. **AI 建议的模块默认不参与提取**：落库为 `ai_suggested`，须转 `custom` 并启用后才生效。

---

## 八、设计决策（3 项 ✅ 新增）

| 决策 | 理由 | 被否决的方案 |
|---|---|---|
| **不用向量相似度作为唯一提取手段** | 精确名称、别名、章节顺序与证据锚点必须始终可得 | 纯向量检索 |
| **先只读诊断，再增量开放** | 避免在应用启动时激进回填；派生项目让原作正典与衍生内容分层 | 启动时全量重建、正典与衍生混写 |
| **结构化地图数据与可审阅视觉稿是两个阶段** | 首个版本不把"地图生图"设为硬依赖 | 需要地图生成器才能用 |

---

## 九、冲突项

无。12 条需求与 50/50 任务、代码、迁移、测试一致。

**2026-09-12 实测记录（fixture 为现场杜撰的合成小说）**：导入 → 逐域检测 9 域 → 提取 `success` →
13 条候选（含证据）→ 接受 1 条耗时 0.07s（**未二次调用模型**）→ `apply` 仅落地已接受的 1 个角色，
其余 12 条仍 `pending`。**真实浏览器（patchright + Chromium）**访问 `/novel-world` 正常渲染，
快照真实出现在 UI；UI 内点「AI 判断模块」经真实 LLM 于 12s 返回逐域判定；`/world-map` 渲染正常；零 >=400 响应。

**一处须记住的观测偏差**：`npm run smoke:pages` 只是 `readFileSync` + `includes()` 的**源码文本扫描**，
既不挂载页面也不启动浏览器——它通过**不代表**页面能运行。

---

## 十、合并计划

| # | 操作 | 目标文件 | 内容概要 |
|---|------|---------|---------|
| 1 | 追加新节「小说来源与世界提取」 | `.ai-sdd/context/business/领域术语与业务对象（domain-glossary）.md` | 7 个术语 |
| 2 | 追加新节 | `.ai-sdd/context/business/业务规则与核验体系（business-rules）.md` | 8 条规则（证据逐字命中、候选先于正典、apply 唯一写入点为核心） |
| 3 | 追加新节 | `.ai-sdd/context/engineering/接口设计规范（api-style-guide）.md` | 端点组 + Agent 工具命名陷阱 |
| 4 | **新建语义子文件** | `.ai-sdd/context/data/核心数据模型（data-model）/小说来源与世界提取（novel-source-model）.md` | 快照/块/候选/实体/地图字段与迁移链 |
| 5 | 追加新节 | `.ai-sdd/context/data/枚举值字典（enum-dictionary）.md` | 5 组枚举 |
| 6 | 追加新节 | `.ai-sdd/context/engineering/设计约束（design-conventions）.md` | 4 条（逐字证据、递归检索、内置字段只增不删、AI 建议默认不生效）+ smoke 的真相 |

---

## 十一、索引更新建议

为 `business-rules` / `design-conventions` / `api-style-guide` / `enum-dictionary` 增加触发，例如：
- `"涉及小说来源导入、来源快照、文本块、混合检索"`
- `"涉及世界设定提取、候选确认、apply 写入、派生项目"`
- `"涉及 WorldEntity / WorldEntityRelation / 世界地图正典与成图边界"`
