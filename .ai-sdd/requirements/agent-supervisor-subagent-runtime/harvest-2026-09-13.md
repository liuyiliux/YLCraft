# 知识回流报告：agent-supervisor-subagent-runtime

- 日期：2026-09-13
- 来源 change：`openspec/changes/agent-supervisor-subagent-runtime/`（**30/30 完成**，待归档）
- 素材：proposal.md（含 Audit Findings 表）/ tasks.md（30 项）/ specs/agent-subagent-runtime/spec.md + 代码与真实浏览器核实
- 代码范围：`services/agent/runtime/delegation.py`、`services/agent/service.py`（`resume_from_delegation_observation`）、
  `services/agent/multi_agent_coordinator.py`、`services/clip/cutclaw_service.py`、`frontend/src/pages/agent|story`
- **注入目标现状**：上下文库已有 Agent 运行域与团队组合的零散记录；本次补**主链机制、边界口径与审计方法**。

---

## 一、回流概览

| 类别 | 新增 | 补充 | 重复（跳过） | 冲突 |
|------|------|------|------------|------|
| 术语与业务对象 | 3 | 0 | 2 | 0 |
| 业务规则 | 5 | 0 | 1 | 0 |
| API | 1 | 0 | 1 | 0 |
| 工程约束 | 5 | 0 | 0 | 0 |
| **合计** | **14** | **0** | **4** | **0** |

---

## 二、术语与业务对象（3 项 ✅ 新增）

| 术语 | 定义 |
|---|---|
| **Supervisor/Worker 主链** | `AgentProfile.can_delegate` 控制 `delegate_agent_tasks` 可见性；`SubagentOrchestrator` 校验**深度、扇出、根预算、依赖、并发**；每个 Worker 使用**独立 Thread + 独立 `AsyncSession` + 独立 `AgentService`** |
| **执行树** | `AgentDelegation` + `AgentRun.root_run_id/parent_run_id` 构成；由 `GET /agent/runs/{run_id}/tree` 与 `/delegations` 暴露 |
| **子代理三原语** | `spawn`（全新会话）/ `fork`（只读父上下文快照）/ `continuable`（续跑同会话，经 `SubagentOrchestrator.send_message`） |

---

## 三、业务规则（5 项 ✅ 新增）

| 规则 | 内容 |
|---|---|
| **子结果汇合后重进父规划** | 汇合结果作为 observation 写回原父 Run，并**继续同一个 `RunLoop`**（`resume_from_delegation_observation`，metadata `phase=delegation_join_observation`） |
| **失败不得降级成文本** | 子失败必须是失败状态，不能变成父级看到的"成功输出" |
| **子确认/取消向父传播** | 汇合时若有子处于 `waiting_confirmation`，join status 即 `waiting_confirmation`，父 Run 状态同步置为该值，并在 summary 计数 |
| **确认后的自动续跑必须由用户触发** | 避免确认接口静默启动新一轮成本型执行 |
| **文案必须匹配真实执行模型** | 确定性顺序服务阶段**必须**表述为 staged workflow；只有**含持久子 Run** 的才可称多智能体，**且须同时暴露 responsible profiles 与执行树作为证据** |

---

## 四、API（1 项 ✅ 新增）

```text
POST /agent/runs/{run_id}/delegate              # 人工委派；body 的 resume_parent 控制是否立即续跑（旧调用默认仅委派，保持兼容）
GET  /agent/runs/{run_id}/tree                  # 执行树
GET  /agent/runs/{run_id}/delegations           # 委派记录
POST /agent/multi-agent/scene-simulation        # 现始终走 TeamComposer("scene-sim")
```

---

## 五、工程约束与踩坑（5 项 ✅ 新增）

1. **审计文案要防"反向失真"**：本次在 `docs/agent/agent-center.md` 与架构文档发现，**过时描述的方向是"低报"**（称"Writer Room 仍是单模型推演""Coordinator 尚未迁移"），而两条均已不成立——**过时不只意味着高估，也可能是低估**；审计必须双向核对。
2. **首屏空态会让 UI 断言误报**：`/agent` 与 `/story` 在无会话/未选项目时正文仅约 1k 字符，**委派入口与团队模式控件均不可见**。断言必须先驱动到"有会话/进入写作室"的状态，否则会把"没进入状态"误判成"功能缺失"。
3. **默认折叠的内容不在 `inner_text` 里**：运行执行过程位于默认折叠的 `<details>` 中，`inner_text` 取不到；**须查 DOM（`document.body.innerHTML`）或先 `details.open = true`**。
4. **统计 Python 测试函数必须匹配 `^(async )?def`**：本会话我因此**误判两次**（漏掉 `test_delegation_primitives.py` 的 3 例与 `test_agent_center.py` 的 parent-resume/confirmation 用例）。`^def` **匹配不到 `async def`**。
5. **`AgentService(session)` 只需一个 session**，内部自建 `ThreadManager`/`MemoryManager`/`AgentProfileManager` 等；测试可直接构造（`test_agent_center.py` 用了 ~30 次），无需手工拼装管理器。

---

## 六、冲突项

无。30/30 任务完成，`openspec validate --strict` 通过。

**2026-09-13 核实与验证**：
- **Phase 4 六项（4.1–4.6）经代码核实均已实现**（此前未勾）：`rehearsal_mode` 默认 `team` 且三层贯通；`_resolve_team_characters`；`writer-room-team` 经 `TeamComposer` 执行且**每角色一个独立子 Agent**（原注释 `Extract each role-actor's independent output (one child per character)`）、每条带 `child_run_id`；Story UI 有模式控件与 `TeamRehearsalPanel`；`MultiAgentCoordinator` 已迁为声明式门面。
- **5.1**：域内 agent 循环实测**只有 CutClaw 一处**，且已通过 `start_cutclaw_clip` 工具与日志接入 → 保持独立、不纳入 Supervisor 语义。
- **5.2**：前端仅 1 处"多智能体"文案且**准确**；**无文档**把 Writer Room 线性流水线误称多智能体（Phase 0.1 整改有效）。
- **5.3**：五项均有专项测试，`48 passed`；补强 4 例 `joined_observation` 状态呈现测试（确认/取消/跳过/失败不得渲染成"已完成"）。
- **5.4**：`npm run build` 通过（21.53s）；真实浏览器确认**执行过程内联**（`13 步 / 5 工具 / 12 成功 / 1 待确认`）与**预算/并发诊断**；**Writer Room 模式控件完整确认**（`角色团队推演（每角色子智能体）` / `快速演绎（单模型）`）；0 个 >=400、0 条控制台 error。
- **5.5**：修正三处过时状态（架构文档边界段、架构文档状态表 19/30→30/30、`docs/README.md` 状态行）。

**唯一保留的运行验证缺口**：Writer Room `team` 模式本身尚未在真实项目上跑过一次（编排机制已由 `scene-sim` 真实验证）。

---

## 七、合并计划

| # | 操作 | 目标文件 |
|---|------|---------|
| 1 | 追加「Supervisor/Worker 主链与执行树」术语 | `business/领域术语与业务对象（domain-glossary）.md` |
| 2 | 追加 5 条规则（含文案准确性要求） | `business/业务规则与核验体系（business-rules）.md` |
| 3 | 追加委派/执行树端点 | `engineering/接口设计规范（api-style-guide）.md` |
| 4 | 追加 5 条工程约束（含两条审计方法与 async def 检索陷阱） | `engineering/设计约束（design-conventions）.md` |

---

## 八、索引更新建议

- `"涉及 Agent 委派、子代理、执行树、父 Run 续跑"`
- `"审计 UI 文案与文档是否夸大或低估多智能体能力"`
- `"统计测试函数（注意 async def 陷阱）"`
