# Image-to-3D Workspace

Provide a standalone, configured-provider image-to-3D workspace. It must create durable tasks and import completed model files into Asset Hub with source-image lineage, rather than relying on the legacy environment-keyed TripoSR helper.
