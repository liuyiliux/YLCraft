# Tasks

## Phase 1: 前端重复请求

- [x] 1. 为模糊搜索与混合搜索增加按参数键的 in-flight Promise 复用，避免 StrictMode 和重复 effect 并发发同一请求。
  - 请求键包含页码、搜索词、过滤条件和选中标签；`finally` 删除键，失败后仍可重试。
- [x] 2. 整理请求去重代码的缩进与加载状态收敛，保持原有错误提示和静态绑骨本地过滤不变。

## Phase 2: 后端读放大

- [x] 3. 无类型且无后置过滤时，改用 `list_all_types` 一次跨类型分页，不再按 13 个 `AssetType` 各查 count + 当前页。
- [x] 4. 最新版本与主表示改为数据库窗口函数，每节点只返回最新版本、每版本只返回最大文件表示。
- [x] 5. 有后置过滤时保留多类型候选语义，避免全局分页后 Python 过滤造成漏数据。
  - `platform`、`source_type`、`status`、`tags`、`project_id`、`asset_role`、`source_stage` 任一存在时走候选路径。

## Phase 3: 归属与分页正确性

- [x] 6. 将归属过滤下沉到 count 与分页 SQL；会话用户看"本人 + NULL 遗留数据"，匿名只看 NULL 遗留数据。
  - 避免分页后 Python 过滤造成登录用户页内卡片不足或空页。
- [x] 7. 修复"快速分页回归"：把可出卡条件下沉到 count 与分页 SQL。
  - 踩坑：只加"存在任意版本且存在任意表示"仍不够。真实数据里 21 个软删除节点和 1 个"最新版本无表示"
    的节点仍会占页，第一页只有 8 张。口径必须与 `_asset_hub_card` 完全一致：排除软删除节点，
    并要求**最新版本**存在文件表示。
  - 实测（root 会话）：修复前 page1=8 / page2=22，修复后 page1=24 / page2=24，total=207。
  - 根因归属：慢查询修复引入的全局分页让"节点名额"直接等于页大小，原先按 13 类取候选时多取的候选
    掩盖了这些跳过，所以这是上次性能修复暴露出的口径不一致，不是独立的分页参数错误。

## Phase 4: 回归测试

- [x] 8. 测试一个节点有 v1/v2/v3 时 `get_latest_versions` 只返回 v3。
- [x] 9. 测试一个版本有多个表示时 `get_primaries` 只返回最大文件表示。
- [x] 10. 测试无类型且无后置过滤只调用一次全局分页，不调用逐类型查询。
- [x] 11. 测试有后置过滤时仍覆盖全部 `AssetType`，保留原有候选语义。
- [x] 12. 测试分页请求带 `require_renderable=True`，确保可出卡条件与卡片组装口径一致。

## Phase 5: 验证与记录

- [x] 13. 运行聚焦后端测试、前端类型检查、单测和生产构建。
- [x] 14. 真实浏览器复测：同一页面加载只发 1 个 `/api/v1/assets?page=1&page_size=24`。
  - 最终抓包 `duration_ms=284.52`、`status=200`、单请求；修复前同页 4 个重复请求且单请求 6.98s–16.17s。
  - 修正：当时页面显示 238 是“全部节点数”。可出卡口径修正后应显示 207，且第一页为 24 张。
  - 本次未重新驱动浏览器（本机 `bsk` CLI 不在 PATH，无法借用已登录会话）；用后端真实库直连复现验证
    第 1/2 页均为 24 张。
- [x] 15. 运行 `openspec validate asset-library-list-performance --strict`。
- [x] 16. 修复"切换每页条数无反应"：分页大小从常量改为状态。
  - 踩坑：父组件 `pageSize` 是常量 24，`AssetGrid` 的 `onPageChange` 第二个参数被忽略，
    选 12/48/96 时请求仍按 24 拉，表现为点了没变化。
  - 处理：页大小改为 state，切换页大小时重置到第 1 页（旧页码在新页大小下可能越界），
    并把 `pageSize` 加入初始加载 effect 依赖。
  - 验证：前端类型检查通过，`vitest` 20 文件 333 测试通过，生产构建通过。
